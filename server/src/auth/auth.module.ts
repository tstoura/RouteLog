import { Module } from '@nestjs/common'

// Phase 3 will add: AuthController, AuthService, JwtStrategy, JwtAuthGuard,
// register/login DTOs, argon2id password hashing, and JWT signing.
@Module({})
export class AuthModule {}
