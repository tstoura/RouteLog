import { Module } from '@nestjs/common'

// Phase 4 will add: ClubsController (GET /clubs, GET /clubs/:id/members),
// ClubsService, MembershipsController (POST /memberships),
// MembershipsService, and related DTOs.
@Module({})
export class ClubsModule {}
